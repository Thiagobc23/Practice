pandas - exploring data frames with pandas
sqlite - simple implementations of sqlite in python. Creating, reading and updating a db in memory, with a .db and .csv file
numpy - exploring the numpy and matplotlib libraries

